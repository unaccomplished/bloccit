You have favorited your post "<%= @post.title %>" and will receive updates when it's commented on
<%= "=" * (@post.title.length + 12 ) %>

By: <%= @post.user.name %>

<%= @post.body %>

Visit online at: <%= topic_post_url(@post.topic, @post) %>